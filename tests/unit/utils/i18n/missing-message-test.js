import i18nMissingMessage from 'static-page-compiler/utils/i18n/missing-message';
import { module, test } from 'qunit';

module('Unit | Utility | i18n/missing message');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = i18nMissingMessage();
  assert.ok(result);
});
