<div class="header" {{action 'toggleVisibility'}}>{{ t 'Template' }}
    {{ fa-icon (concat 'angle-' (if hidden 'down' 'up'))
        class="clickable"
    }}
</div>
{{#unless hidden }}
    {{ textarea class="form-control" value=template }}
{{/unless }}
